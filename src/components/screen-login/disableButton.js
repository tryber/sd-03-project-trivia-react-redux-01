export const disableButton = () => {
const buttonEmail = document.getElementById('input-gravatar-email')
const buttonName = documen.getElementById('input-player-name')
if(buttonEmail === ''){
  <input type='email' disabled />
}
if(buttonName === ''){
  <input type='text' disabled />
}
}